// my-portfolio/vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  // This tells Vite where your root HTML and static assets are located for development.
  root: './web',

  // build: {
  //   // This defines where Vite will output your production build.
  //   // By default it's 'dist', but here we are saying it will be in 'my-portfolio/dist'
  //   // relative to the project root, regardless of the 'root' setting.
  //   outDir: 'dist'
  // }
});